id: sine
label: sine

parameters:
-   id: name
    label: name
    dtype: string
    default: none

inputs:
-   domain: stream
    label: x
    dtype: float

outputs:
-   domain: stream
    label: y
    dtype: float

outvars:
- x
- y
