id: dummy_sink
label: dummy sink

parameters:
-   id: n
    label: n
    dtype: string
    default: none

inputs:
-   domain: stream
    dtype: float

documentation: |-
    To be used along with dummy source.

