id: diff
label: diff

parameters:
-   id: name
    label: name
    dtype: string
    default: none

inputs:
-   domain: stream
    label: x1
    dtype: float
-   domain: stream
    label: x2
    dtype: float

outputs:
-   domain: stream
    label: y
    dtype: float

outvars:
- x1
- x2
- y
